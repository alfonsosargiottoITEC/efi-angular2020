import { BalancepipePipe } from './balancepipe.pipe';

describe('BalancepipePipe', () => {
  it('create an instance', () => {
    const pipe = new BalancepipePipe();
    expect(pipe).toBeTruthy();
  });
});
